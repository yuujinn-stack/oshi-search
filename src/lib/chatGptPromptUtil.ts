/**
 * ChatGPT向けプロンプトの末尾に追加する「ダウンロードCSV必須」セクション。
 * CSVを依頼するすべてのプロンプト生成関数に共通で使用する。
 */
export function csvDownloadSection(filename: string): string {
  return `
━━━━━━━━━━━━━━━━━━
重要
━━━━━━━━━━━━━━━━━━

回答は必ず以下の順番で出力してください。

① ダウンロード可能なCSVファイルを生成してください。

② 同じ内容をCSVコードブロックでも表示してください。

CSVファイルは必須です。
コードブロックだけで終了しないでください。
ChatGPTのダウンロード機能で保存できるCSVファイルを必ず生成してください。

CSVファイル名：${filename}

CSVコードブロックとダウンロードCSVの内容を完全に一致させてください。`;
}
